/* Global variables for LHa core logic */
#define LHA_MAIN_SRC
#include "lha.h"

/* The following are defined in lha.h as EXTERN */
/* They are instantiated here because lharc.c is excluded from the library. */

/* (No additional code needed here as include "lha.h" with LHA_MAIN_SRC will define them) */
